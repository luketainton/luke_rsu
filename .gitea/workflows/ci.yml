name: Unified Python CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: actions/gha-workflows/.gitea/workflows/ci-python-unified.yml@main
    with:
      python-version: "3.14"
      package-manager: uv
      docker-enabled: true
